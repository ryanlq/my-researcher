# GPT-Researcher 文档研究总结

基于官方文档的深度分析

---

## 一、Context（上下文）机制

### 1.1 数据来源类型

GPT-Researcher 支持 **5 种上下文来源**：

| 来源类型 | 说明 | 使用场景 |
|---------|------|----------|
| **web** | 默认，从互联网搜索 | 通用网络研究 |
| **local** | 本地文档 | 企业知识库、个人文档 |
| **static** | 指定 URL 列表 | 针对特定网站研究 |
| **langchain_documents** | LangChain Document 对象 | 程序化数据导入 |
| **langchain_vectorstore** | 向量数据库 | RAG 应用、知识库 |
| **hybrid** | 混合模式 | 本地 + 网络结合 |

---

### 1.2 核心工作流程

```
Step 1: 内容转换为 LangChain Documents
    ↓
Step 2: Documents 存入 VectorStore
    ↓
Step 3: 传递 VectorStore 给 GPT-Researcher
```

**关键技术**：
- 使用 **LangChain Documents** 作为标准数据格式
- 使用 **LangChain VectorStores** 进行语义检索
- 支持所有 LangChain 兼容的向量数据库（FAISS、PGVector、Chroma 等）

---

### 1.3 具体用法示例

#### (1) 指定来源研究

```python
from gpt_researcher import GPTResearcher

researcher = GPTResearcher(
    query="AI 最新趋势",
    report_type="research_report",
    source_urls=[                          # 指定来源
        "https://en.wikipedia.org/...",
        "https://www.ibm.com/..."
    ],
    complement_source_urls=False           # 仅使用指定来源
)
```

#### (2) 本地文档研究

```python
# 环境变量
# DOC_PATH="./my-docs"

researcher = GPTResearcher(
    query="基于我的文档分析...",
    report_source="local"                  # 本地模式
)
```

**支持格式**：PDF、TXT、CSV、Excel、Markdown、PPT、Word

#### (3) 向量数据库研究

```python
from langchain_postgres.vectorstores import PGVector
from langchain_openai import OpenAIEmbeddings

vector_store = PGVector.from_existing_index(
    collection_name='my_docs',
    connection=CONNECTION_STRING,
    embeddings=OpenAIEmbeddings()
)

researcher = GPTResearcher(
    query="查询问题",
    report_source="langchain_vectorstore",  # 向量库模式
    vector_store=vector_store
)
```

#### (4) 混合模式

```python
researcher = GPTResearcher(
    query="研究问题",
    report_source="hybrid",                # 本地 + 网络
    vector_store=vector_store,             # 本地向量库
    query_domains=["wikipedia.org", ...]   # 域名过滤
)
```

---

### 1.4 数据摄取最佳实践

**何时使用自定义数据摄取流程**：
- 嵌入模型达到 API 速率限制
- VectorStore 需要速率限制
- 需要自定义限流/节流逻辑

**步骤**：
1. 转换内容为 LangChain Documents
2. Documents 分块（建议 chunk_size=200, overlap=30）
3. 添加 metadata（source、title、file_path 等）
4. 批量插入 VectorStore（建议每批 100 条）

---

### 1.5 域名过滤

**支持的检索器**：
- ✅ Tavily
- ✅ Google Search

**使用方式**：

```python
# 代码方式
researcher = GPTResearcher(
    query="AI 创业公司",
    query_domains=["forbes.com", "techcrunch.com"]
)

# URL 参数方式
https://app.gptr.dev/?domains=wikipedia.org,wired.com
```

---

## 二、Multi-Agents（多代理）架构

### 2.1 LangGraph 多代理系统

GPT-Researcher 提供了一个基于 **LangGraph** 的多代理协作系统，灵感来自 [STORM 论文](https://arxiv.org/abs/2402.14207)。

---

### 2.2 代理团队构成（7 个 AI 代理）

| 角色 | 职责 | 功能描述 |
|------|------|----------|
| **Human** | 人工监督 | 提供反馈，监督流程 |
| **Chief Editor** | 主编/协调器 | 协调所有代理，管理团队 |
| **Researcher** | 研究员 | 执行深度研究（gpt-researcher） |
| **Editor** | 编辑 | 规划研究大纲和结构 |
| **Reviewer** | 审稿人 | 验证结果正确性，提供反馈 |
| **Revisor** | 修订者 | 根据反馈修订内容 |
| **Writer** | 作者 | 编写最终报告 |
| **Publisher** | 发布者 | 发布多种格式报告 |

---

### 2.3 工作流程（5 个阶段）

```
1. 规划阶段 (Planning)
    └─→ Editor 规划报告结构

2. 数据收集分析 (Data Collection)
    └─→ Researcher 深度研究（并行处理各子主题）

3. 审查修订 (Review & Revision)
    ├─→ Reviewer 验证
    └─→ Revisor 修订（循环直到满意）

4. 撰写提交 (Writing)
    └─→ Writer 编写最终报告（引言、结论、参考文献）

5. 发布 (Publication)
    └─→ Publisher 发布（PDF、DOCX、Markdown）
```

---

### 2.4 架构图

```
┌─────────────────────────────────────────────────────┐
│                   Chief Editor                       │
│                  (主协调器)                          │
└─────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Initial    │  │    Editor    │  │   Writer     │
│   Research   │  │  (规划大纲)   │  │  (编写报告)   │
│  (Browser)   │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                │                │
        ▼                ▼                │
┌──────────────┐  ┌──────────────┐        │
│  Researcher   │  │  子主题循环   │        │
│ (深度研究)    │  │  (并行处理)   │        │
└──────────────┘  └──────────────┘        │
        │                │                │
        ▼                ▼                │
   ┌──────────────────────────────┐       │
   │  每个子主题:                 │       │
   │    ├─→ Researcher 研究      │       │
   │    ├─→ Reviewer 验证        │───────┼──→ 最终报告
   │    └─→ Revisor 修订         │       │
   └──────────────────────────────┘       │
                                          │
┌──────────────┐  ┌──────────────┐        │
│  Publisher   │  │  Human       │        │
│ (多格式发布)  │  │ (人工反馈)    │        │
└──────────────┘  └──────────────┘        │
                                          │
                                     (PDF/DOCX/MD)
```

---

### 2.5 配置文件 (task.json)

```json
{
  "query": "AI 是否处于炒作周期？",
  "model": "gpt-4o",
  "max_sections": 3,                    // 最大章节数
  "include_human_feedback": false,       // 是否人工参与
  "source": "web",                       // web/local
  "follow_guidelines": true,             // 遵循指南
  "guidelines": [                        // 报告指南
    "必须完整回答原始问题",
    "使用 APA 格式",
    "使用英语撰写"
  ],
  "publish_formats": {                   // 输出格式
    "markdown": true,
    "pdf": true,
    "docx": true
  },
  "verbose": true
}
```

---

### 2.6 运行方式

#### 方式 1：本地运行

```bash
# 安装依赖
pip install -r requirements.txt

# 配置环境
export OPENAI_API_KEY=xxx
export TAVILY_API_KEY=xxx

# 运行
python main.py
```

#### 方式 2：Docker 部署

```bash
# 构建
docker-compose up --build

# 启动服务
# - Python server: localhost:8000
# - React app: localhost:3000
```

#### 方式 3：LangGraph Cloud

```bash
pip install langgraph-cli
langgraph up
```

---

### 2.7 NextJS 前端集成

**新增功能**：
- 📁 拖拽上传本地文档
- ⚙️ GUI 环境变量配置
- 🤖 触发 multi_agents 流程
- 📊 稳定性修复

---

## 三、Deep Research 模式详解

### 3.1 核心特性

**树状探索模式**：
- **Breadth（广度）**：每层生成多个搜索查询
- **Depth（深度）**：递归深入探索
- **Concurrent（并发）**：异步并行处理
- **Smart Context（智能上下文）**：自动聚合发现

### 3.2 性能指标

| 指标 | 数值 |
|------|------|
| **完成时间** | ~5 分钟 |
| **成本** | ~$0.4 (o3-mini, high reasoning) |
| **输出长度** | 2000+ 词 |
| **深度层数** | 2-3 层（可配置） |
| **广度分支** | 4-5 个（可配置） |
| **并发数** | 4 个（可配置） |

---

### 3.3 配置参数

#### 环境变量

```bash
DEEP_RESEARCH_BREADTH=5         # 每层查询数
DEEP_RESEARCH_DEPTH=3           # 研究深度
DEEP_RESEARCH_CONCURRENCY=4     # 并发数
TOTAL_WORDS=2000               # 报告字数
```

#### 代码配置

```python
from gpt_researcher import GPTResearcher
from gpt_researcher.utils.enum import ReportType

researcher = GPTResearcher(
    query="量子计算最新进展",
    report_type=ReportType.DeepResearch.value,  # 关键！
    max_subtopics=7,
    tone="Analytical"
)

# 执行深度研究
await researcher.conduct_research()

# 生成报告
report = await researcher.write_report()
```

---

### 3.4 进度跟踪

```python
class ResearchProgress:
    current_depth: int       # 当前深度
    total_depth: int         # 总深度
    current_breadth: int     # 当前并行数
    total_breadth: int       # 总并行数
    current_query: str       # 当前查询
    completed_queries: int   # 已完成查询
    total_queries: int       # 总查询数
```

---

### 3.5 最佳实践

1. **Start Broad** - 从宽泛查询开始，让系统探索细节
2. **Monitor Progress** - 使用进度回调了解流程
3. **Adjust Parameters** - 根据需求调整深度和广度
4. **Resource Management** - 考虑系统资源限制

---

## 四、与标准研究的对比

| 特性 | 标准研究 | 深度研究 |
|------|-------------------|------------------|
| **report_type** | `research_report` | `deep` |
| **搜索层数** | 1 层 | 3 层（可配置） |
| **子主题数** | 3-5 个 | 7+ 个 |
| **并发查询** | 低 | 高（可配置） |
| **时间成本** | ~1 分钟 | ~5 分钟 |
| **金钱成本** | ~$0.05 | ~$0.40 |
| **使用 LLM** | 多个混合 | STRATEGIC_LLM (o3-mini) |
| **适用场景** | 快速调研 | 学术/深度分析 |

---

## 五、核心架构洞察

### 5.1 数据流

```
用户查询
    │
    ├─→ [上下文选择]
    │   ├─→ web (网络搜索)
    │   ├─→ local (本地文档)
    │   ├─→ static (指定 URL)
    │   ├─→ vectorstore (向量库)
    │   └─→ hybrid (混合)
    │
    ├─→ [模式选择]
    │   ├─→ standard (单层)
    │   └─→ deep (多层递进)
    │
    ├─→ [研究执行]
    │   ├─→ ResearchConductor (标准)
    │   └─→ DeepResearchSkill (深度)
    │
    ├─→ [多代理协作] (可选)
    │   ├─→ Editor 规划
    │   ├─→ Researcher 研究
    │   ├─→ Reviewer 验证
    │   ├─→ Revisor 修订
    │   └─→ Writer 编写
    │
    └─→ [报告生成]
        └─→ ReportGenerator
            ├─→ markdown
            ├─→ pdf
            └─→ docx
```

---

### 5.2 关键技术栈

| 组件 | 技术栈 |
|------|--------|
| **LLM** | OpenAI, Anthropic, Ollama, 等 20+ |
| **Vector Store** | LangChain VectorStores (FAISS, PGVector, 等) |
| **Embedding** | OpenAI, Cohere, HuggingFace, Ollama |
| **Multi-Agent** | LangGraph |
| **Web Scraper** | BeautifulSoup, Playwright |
| **Retriever** | Tavily, Google, Bing, DuckDuckGo, SearXNG, MCP |
| **Framework** | FastAPI, NextJS |

---

## 六、实战建议

### 6.1 选择合适的模式

**使用标准研究** (`research_report`)：
- ✅ 快速调研
- ✅ 成本敏感
- ✅ 日常信息收集

**使用深度研究** (`deep`)：
- ✅ 学术研究
- ✅ 深度分析
- ✅ 报告/论文撰写
- ✅ 不限成本

**使用多代理** (LangGraph)：
- ✅ 需要人工监督
- ✅ 多轮审稿修订
- ✅ 高质量报告输出

---

### 6.2 成本优化策略

1. **混合 LLM 策略**
   ```python
   FAST_LLM=ollama:llama3.2        # 本地，免费
   SMART_LLM=openai:gpt-4o        # 云端，强大
   EMBEDDING=ollama:nomic-embed    # 本地，节省
   ```

2. **调整深度参数**
   ```bash
   DEEP_RESEARCH_BREADTH=3         # 降低广度
   DEEP_RESEARCH_DEPTH=2           # 降低深度
   DEEP_RESEARCH_CONCURRENCY=2     # 降低并发
   ```

3. **使用本地检索器**
   - DuckDuckGo（免费）
   - SearXNG（本地部署）
   - 而非 Tavily（付费）

---

### 6.3 质量优化策略

1. **增加子主题数**
   ```python
   max_subtopics=10
   ```

2. **使用分析性语气**
   ```python
   tone="Analytical"  # 或 "Critical"
   ```

3. **启用审查修订循环**（LangGraph）
   ```python
   "follow_guidelines": true,
   "include_human_feedback": true
   ```

4. **混合数据源**
   ```python
   report_source="hybrid",
   vector_store=custom_vectorstore,
   query_domains=["arxiv.org", "nature.com"]
   ```

---

## 七、总结

### 优势

✅ **模块化架构** - Skills 组合，易于扩展
✅ **灵活的数据源** - 支持 5 种上下文来源
✅ **LangChain 集成** - 兼容丰富生态
✅ **多代理协作** - LangGraph 实现
✅ **深度研究能力** - 多层递进探索
✅ **成本可控** - 支持本地+云端混合

### 局限

⚠️ **成本较高** - 深度研究 ~$0.4/次
⚠️ **速度较慢** - 深度研究 ~5 分钟
⚠️ **依赖外部服务** - 需要 API Keys
⚠️ **语言限制** - 英文效果最佳

### 适用场景

| 场景 | 推荐配置 |
|------|---------|
| **日常快速调研** | `research_report` + DuckDuckGo |
| **学术研究** | `deep` + 多代理 + `Analytical` 语气 |
| **企业知识库** | `local` + `vectorstore` |
| **RAG 应用** | `langchain_vectorstore` |
| **混合研究** | `hybrid` + 自定义向量库 |
